<UserControl x:Class="FarmReel.App.Views.FlowsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel Margin="4">
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal">
            <Button Content="New" Click="New_Click"/>
            <Button Content="Load Selected" Click="Load_Click"/>
            <Button Content="Validate" Click="Validate_Click"/>
            <Button Content="Save Override" Click="Save_Click"/>
            <Button Content="Delete Custom" Click="Delete_Click"/>
            <TextBlock x:Name="StatusText" Text="{Binding Status}" VerticalAlignment="Center" Margin="8,0,0,0" Foreground="#2B5797"/>
        </StackPanel>
        <TextBlock DockPanel.Dock="Top" Text="Flows are data-driven UI automation scripts. Facebook UI changes are fixed by editing a flow JSON here - no rebuild needed. Use ops: startApp, forceStop, waitFor, waitForText, tap, tapIf, tapIfTrue, longPress, swipe, scrollTo, type, verify, back, openLink, selectFiles, commentOnPost, dumpText, pullAppData, screenshot, sleep, log." TextWrapping="Wrap" Margin="4,4,4,6" Foreground="Gray"/>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="230"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <ListBox x:Name="FlowList" ItemsSource="{Binding FlowNames}" SelectionChanged="FlowList_SelectionChanged"/>
            <TextBox Grid.Column="1" x:Name="Editor" Text="{Binding CurrentJson, UpdateSourceTrigger=PropertyChanged}"
                     AcceptsReturn="True" AcceptsTab="True" FontFamily="Consolas" FontSize="12"
                     VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Margin="4,0,0,0"/>
        </Grid>
    </DockPanel>
</UserControl>
